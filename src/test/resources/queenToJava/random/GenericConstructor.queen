public final implementation GenericConstructor<K extends Comparable, V> of Entry{
    private K key;
    private V value;

    public <K extends Comparable, V> MapEntry(K key, V value) {
        this.key = key;
        this.value = value;
    }

    @Override
    public <K> boolean contains(K key) {
        return true;
    }

    @Override
    public <R extends V> V get(K key) {
        return true;
    }
}